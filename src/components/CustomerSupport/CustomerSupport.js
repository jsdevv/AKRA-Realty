import React from 'react';
import './CustomerSupport.css'; // Import the CSS for styling

const CustomerSupport = () => {
  return (
    <div className="customer-support-section-unique">
      <div className="overlay-unique">
        <div className="support-content-unique">
          <h1 className="support-title-unique">Dedicated Customer Support</h1>
          <p className="support-description-unique">We are here to help you with all your needs 24/7.</p>
        </div>
      </div>
    </div>
  );
};

export default CustomerSupport;
